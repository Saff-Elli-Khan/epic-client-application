import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.oridune.app",
  appName: "oridune",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
